// Stub — Socket.io server (match rooms, scoreUpdate broadcasts) implemented in Phase 6
export function initSocket(httpServer) {
  // TODO: Phase 6 — attach socket.io to the Fastify HTTP server, handle join-match rooms
}
