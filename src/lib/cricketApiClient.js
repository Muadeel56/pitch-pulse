// Stub — real client (getLiveMatches, getMatchDetail) implemented in Phase 3, against
// either a real free-tier cricket API key or the mock data generator fallback (see README).
export const cricketApiClient = {};
